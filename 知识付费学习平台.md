## 项目主要需求

### 项目概述

构建一个知识付费学习平台，面向内容创作者和终身学习者。创作者可发布付费专栏、音频课程和电子书，用户按需订阅购买后即可学习。平台整合积分商城体系，用户通过学习行为获取积分并兑换课程优惠，形成学习激励闭环。

### 功能需求

1. **创作者入驻**：创作者注册后提交个人资料和领域专长，管理员审核通过后获得内容发布权限，创作者可自定义个人主页（头像、简介、擅长领域标签、社交链接）
2. **专栏订阅**：创作者发布付费专栏（如"深入浅出 Java 设计模式"），专栏包含多篇文章按时间顺序更新，用户按专栏订阅（支持月付、季付、年付），已订阅用户可查看专栏全部历史文章
3. **音频课程**：创作者上传音频课程（支持单集和多集系列），每集音频包含标题、简介和时长信息，在线播放支持进度条拖拽、倍速播放（0.75x/1.0x/1.5x/2.0x）和定时关闭，支持后台播放（页面切换不中断）
4. **电子书阅读**：创作者上传电子书（PDF/EPUB 格式），平台提供在线阅读器（支持翻页、字体大小调节、夜间模式、书签标记、阅读进度记忆），电子书支持试读（前 10% 免费）后付费购买
5. **支付与订单**：用户购买专栏、音频课或电子书，支持支付宝沙箱环境支付，订单管理支持查看历史购买记录和发票申请
6. **积分商城**：用户通过学习行为获取积分——每日签到（+5 分）、完成课程学习（+10 分）、发表优质评论（+20 分），积分可在商城兑换优惠券（如 100 积分兑换 10 元优惠券）或免费课程
7. **内容推荐与搜索**：首页推荐热门专栏、新品课程和编辑精选，支持按关键词全局搜索内容（专栏文章、音频、电子书），搜索结果按相关度和热度排序

### 技术要求

- 前端：Next.js 框架（App Router），使用 Ant Design 组件库，Vite 构建工具
- 后端：Spring Boot 框架，Java 17 开发
- 数据库：MongoDB，使用 Spring Data MongoDB
- 缓存：Redis 用于积分系统、热门内容缓存和排行榜
- 对象存储：MinIO 存储音频文件、电子书和图片资源
- 认证方式：JWT 认证
- 支付：支付宝沙箱环境集成
- 搜索：MongoDB 全文检索实现内容搜索

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：

页面，组件，路由,状态管理,API 请求,类型定义,样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：

数据库脚本放在 `database/`。

## 部署要求

项目必须支持 Docker Compose 一键部署。

需要提供以下文件：

- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目构建与运行镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明

docker-compose.yml 要求：

- 所有服务使用命名容器，便于管理
- 数据库数据使用命名卷持久化
- 前端通过 Nginx 反向代理后端 API
- 服务间通过 Docker 内部网络通信
- 数据库服务配置 healthcheck，后端服务依赖数据库健康检查通过后再启动
- 暴露端口（避免与其他项目冲突）：前端 8015，后端 3015，MongoDB 2804，Redis 6415，MinIO 9013/9014
- 支持通过 `docker compose up -d` 一条命令启动全部服务

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：

- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：

- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

